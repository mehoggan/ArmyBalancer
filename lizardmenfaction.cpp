#include "lizardmenfaction.h"

namespace WarScrollGeneration
{
  WarScroll Lord_Kroak(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Slann_Mage_Priest(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Kroq_Gar_on_Carnosaur(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Saurus_Oldblood(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Saurus_Oldblood_on_Cold_One(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Chakax_the_Eternity_Warden(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Gor_Rok(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Scar_vetran_with_Battle_Standard(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Scar_vetran_on_Carnosaur(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Saurus_Warriors(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Temple_Guard(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Saurus_Cavalry(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Tehenhauin_Prophet_of_Sotek(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Tetto_eko(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Oxyotl(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Skink_Priest(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Skink_Chief(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Skinks(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Chameleon_Skinks(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Terradon_Riders(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Ripperdactyl_Riders(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Skink_Handlers(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Salamanders(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Razordons(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Kroxigor(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Stegadon(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Engine_of_the_Gods(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Bastiladon(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Troglodon(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }

  WarScroll Jungle_Swarms(const QString &name)
  {
    qDebug() << "Generating WarScroll for " << name;
    WarScroll ws;
    ws.setTitle(name);
    return ws;
  }
}

LizardmenFaction::LizardmenFaction() :
  IFaction(nullptr)
{
  m_Units.push_back(tr(""));
  m_Units.push_back(tr("Lord Kroak"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Lord_Kroak, m_Units.back()));
  m_Units.push_back(tr("Slann Mage-Priest"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Slann_Mage_Priest, m_Units.back()));
  m_Units.push_back(tr("Kroq-Gar on Carnosaur"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Kroq_Gar_on_Carnosaur, m_Units.back()));
  m_Units.push_back(tr("Saurus Oldblood"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Saurus_Oldblood, m_Units.back()));
  m_Units.push_back(tr("Saurus Oldblood on Cold One"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Saurus_Oldblood_on_Cold_One, m_Units.back()));
  m_Units.push_back(tr("Chakax, the Eternity Warden"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Chakax_the_Eternity_Warden, m_Units.back()));
  m_Units.push_back(tr("Gor-Rok"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Gor_Rok, m_Units.back()));
  m_Units.push_back(tr("Scar-vetran with Battle Standard"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Scar_vetran_with_Battle_Standard, m_Units.back()));
  m_Units.push_back(tr("Scar-vetran on Carnosaur"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Scar_vetran_on_Carnosaur, m_Units.back()));
  m_Units.push_back(tr("Saurus Warriors"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Saurus_Warriors, m_Units.back()));
  m_Units.push_back(tr("Temple Guard"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Temple_Guard, m_Units.back()));
  m_Units.push_back(tr("Saurus Cavalry"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Saurus_Cavalry, m_Units.back()));
  m_Units.push_back(tr("Tehenhauin, Prophet of Sotek"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Tehenhauin_Prophet_of_Sotek, m_Units.back()));
  m_Units.push_back(tr("Tetto'eko"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Tetto_eko, m_Units.back()));
  m_Units.push_back(tr("Oxyotl"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Oxyotl, m_Units.back()));
  m_Units.push_back(tr("Skink Priest"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Skink_Priest, m_Units.back()));
  m_Units.push_back(tr("Skink Chief"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Skink_Chief, m_Units.back()));
  m_Units.push_back(tr("Skinks"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Skinks, m_Units.back()));
  m_Units.push_back(tr("Chameleon Skinks"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Chameleon_Skinks, m_Units.back()));
  m_Units.push_back(tr("Terradon Riders"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Terradon_Riders, m_Units.back()));
  m_Units.push_back(tr("Ripperdactyl Riders"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Ripperdactyl_Riders, m_Units.back()));
  m_Units.push_back(tr("Skink Handlers"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Skink_Handlers, m_Units.back()));
  m_Units.push_back(tr("Salamanders"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Salamanders, m_Units.back()));
  m_Units.push_back(tr("Razordons"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Razordons, m_Units.back()));
  m_Units.push_back(tr("Kroxigor"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Kroxigor, m_Units.back()));
  m_Units.push_back(tr("Stegadon"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Stegadon, m_Units.back()));
  m_Units.push_back(tr("Engine of the Gods"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Engine_of_the_Gods, m_Units.back()));
  m_Units.push_back(tr("Bastiladon"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Bastiladon, m_Units.back()));
  m_Units.push_back(tr("Troglodon"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Troglodon, m_Units.back()));
  m_Units.push_back(tr("Jungle Swarms"));
  m_NameToWarScroll.insert(m_Units.back(),
    std::bind(&WarScrollGeneration::Jungle_Swarms, m_Units.back()));
}

const QStringList &LizardmenFaction::getUnits() const
{
  return m_Units;
}

QList<WarScroll> LizardmenFaction::getWarScrolls() const
{
  QList<WarScroll> warScrolls;
  for (auto ws : m_NameToWarScroll.toStdMap()) {
    warScrolls.push_back(ws.second(ws.first));
  }
  return warScrolls;
}
